{
    "packageManager": "pnpm@10.8.1",
	"private": true,
	"devDependencies": {
		"turbo": "^2.8.4"
	},
	"scripts": {
		"build": "dotenv -- turbo build",
		"dev": "dotenv -- turbo watch lint dev",
		"lint": "dotenv -- turbo lint",
		"test": "dotenv -- turbo test"
	}
}
